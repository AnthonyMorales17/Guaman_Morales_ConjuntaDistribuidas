apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: guamanmorales-postgres-pvc
  namespace: guamanmorales
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
