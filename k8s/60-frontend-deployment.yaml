apiVersion: apps/v1
kind: Deployment
metadata:
  name: guamanmorales-frontend
  namespace: guamanmorales
  labels:
    app: guamanmorales-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guamanmorales-frontend
  template:
    metadata:
      labels:
        app: guamanmorales-frontend
    spec:
      containers:
        - name: frontend
          image: guamanmorales-frontend:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "128Mi"
              cpu: "200m"
